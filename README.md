# Kingfall — desktop releases

Binary-only branch (no source). Latest Windows alpha:

- **Kingfall-Setup.exe** — the installer (per-user, no admin needed)
- **Kingfall-win64.zip** — portable: extract and run Kingfall.exe

Built from the UNITY-VERSION branch. SmartScreen note: the exe is unsigned;
click "More info → Run anyway" the first time.
